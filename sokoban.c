#include <stdio.h>
int main(void)
{
  pos=ftell(sokoban_ptr);   //현재 위치 유지

        ch f;        
	ch s;        
	f=getc(sokoban);        
	s=getc(sokoban);

if(ch==f){
	FILE *sokoban;
	sokoban =fopen("sokoban","w");
}
if(ch==s){
	FILE *sokoban;
	fclose(sokoban);}

	return 0;
	}

